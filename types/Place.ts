type Place = {
    placeId: number | null,
    locationId: number,
    name: string,
    address: string,
    latitude: number,
    longitude: number,
    imageUrl: string,
    time: string,
};

export default Place; 